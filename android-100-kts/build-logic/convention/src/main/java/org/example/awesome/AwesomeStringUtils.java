package org.example.awesome;

// Class that helps trigger build logic ABI and non-ABI changes
// It is intentionally written in Java
public class AwesomeStringUtils {

    public static String trim(String s) {
        return s.trim();
    }
}
